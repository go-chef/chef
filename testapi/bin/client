#!/bin/bash

# Client testing

BASE=$(dirname $0)

. ${BASE}/setup
. ${BASE}/creds
go run -tags debug ${BASE}/../testcase/testcase.go client ${CHEFUSER} ${KEYFILE} ${CHEFORGANIZATIONURL} ${SSLBYPASS} '1.0'
