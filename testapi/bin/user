#!/bin/bash

# Group testing

BASE=$(dirname $0)

. ${BASE}/setup
. ${BASE}/creds
# Use the organization end point and let the GlobalBaseUrl code figure out the right URL
go run ${BASE}/../testcase/testcase.go user ${CHEFUSER} ${KEYFILE} ${CHEFORGANIZATIONURL} ${SSLBYPASS}
